# Task 1: Create the Windows Unit-Test Harness

## Objective

Create a real Windows xUnit test project that loads the actual Razor Reaper application assembly and provides dependency-free reusable test infrastructure for the remaining privacy/startup tasks. This task changes no production behavior.

## Owned files

- Modify `RazorReaper.sln`.
- Modify `RazorReaper/RazorReaper.csproj` only for test assembly visibility.
- Create `tests/RazorReaper.UnitTests/RazorReaper.UnitTests.csproj`.
- Create `tests/RazorReaper.UnitTests/GlobalUsings.cs`.
- Create `tests/RazorReaper.UnitTests/SmokeTests.cs`.
- Create dependency-free infrastructure under `tests/RazorReaper.UnitTests/Infrastructure/`:
  - `FakePreferencesStore.cs`
  - `FakeOsLocationProvider.cs`
  - `RecordingHttpMessageHandler.cs`
  - `ManualTimeProvider.cs`
- Write ignored/local report `docs/superpowers/.sdd/2026-08-16-app-launch-privacy-plan/task-1-report.md`.

Do not modify any other production source, App behavior, package version, updater, telemetry, location, Discord, access, UI, installer, or Shop file.

## Environment and binding safety rules

- Work only in `C:\Users\cedri\source\repos\CedrickGD\RazorReaper\.claude\worktrees\navbar-search-usability-f99f82` at detached `HEAD`.
- Do not create/switch a branch, commit, merge, push, publish, deploy, launch the App, access a remote service, or spawn subagents.
- Use `apply_patch` for hand edits. `dotnet new`, `dotnet sln add`, and `dotnet add reference` are allowed only for their exact mechanical Task 1 outputs; inspect and normalize their result immediately.
- The baseline App Debug build already passes with `--no-restore`. Preserve it.
- All test/build verification uses `--no-restore`.
- A new test project needs `obj/project.assets.json`. If it is absent after scaffolding, one assets-generation restore is allowed only after confirming every exact nupkg exists locally, and it must name only an explicit local hierarchical package source so NuGet has no configured remote feed to contact. Do not use ordinary restore, `--ignore-failed-sources`, or any command that can fall back to nuget.org. Record the exact local-only command/output in the report. If local resolution fails, stop as BLOCKED rather than enabling network.
- The installed SDK template itself requests uncached newer packages. Normalize the generated project to the cached versions below before any restore. The controller verified the net10-relevant direct/transitive `.nupkg` closure is local.
- For the one restore, create a temporary config outside the repository at `C:\Users\cedri\AppData\Local\Temp\razorreaper-unit-tests-local-only.config` containing `<packageSources><clear/><add key="local-cache" value="C:\Users\cedri\.nuget\packages"/></packageSources>`, use it with `--configfile` and `-p:NuGetAudit=false`, then delete that temporary config with `apply_patch` after successful assets generation. No other source may be named or consulted.

## Test-project contract

- Target `net10.0-windows10.0.19041.0`.
- `IsTestProject=true`, `IsPackable=false`, nullable enabled, implicit usings enabled.
- Use the locally available exact template package set, without adding a mocking package:
  - `Microsoft.NET.Test.Sdk` 17.12.0
  - `xunit` 2.9.2
  - `xunit.runner.visualstudio` 2.8.2 with the template's normal private/include assets
  - `coverlet.collector` 6.0.2 with the template's normal private/include assets
- Add one project reference to `../../RazorReaper/RazorReaper.csproj`.
- Add the test project to `RazorReaper.sln` with Debug/Release Any CPU mappings generated by the solution tooling.
- Add only `InternalsVisibleTo("RazorReaper.UnitTests")` to the App project using an SDK-supported project item or a narrowly scoped assembly attribute. Do not broaden visibility or add signing workarounds.
- Delete the xUnit template's `UnitTest1.cs` if it is generated.
- Remove the template's `<Using Include="Xunit" />` and keep `GlobalUsings.cs` as the single xUnit global-using owner. `MauiProgram` is already public, so InternalsVisibleTo is for the later policy tests, not a precondition for this smoke test.

## Strict TDD sequence

1. Scaffold the test project without restore, but defer the App project reference and solution wiring.
2. Normalize its target framework/properties/packages, add `GlobalUsings.cs`, and write `SmokeTests.AppAssemblyLoads` with the exact assertion that `typeof(MauiProgram).Assembly.GetName().Name` is `RazorReaper`.
3. Run:

   `dotnet test .\tests\RazorReaper.UnitTests\RazorReaper.UnitTests.csproj -c Debug --no-restore --filter "FullyQualifiedName~SmokeTests"`

   Observe RED from the missing App integration/project wiring (or missing assets before the narrowly local restore). The failure must be explained and must not be a typo.
4. If needed, generate assets through the ruled local-only source. Re-run RED until the test reaches the intended missing-reference/wiring failure.
5. Add the App project reference, solution entry, and test assembly visibility. Make no other production change.
6. Run the same filtered command and observe one passing smoke test.
7. Refactor only generated noise while green.

## Infrastructure contract

These fakes must compile without pulling future production interfaces into Task 1:

- `FakePreferencesStore`: deterministic in-memory key/value storage with clear Get/Set/Remove semantics suitable for later adaptation; no MAUI static API.
- `FakeOsLocationProvider`: a minimal dependency-free call recorder/programmable result seam; do not invent the final production location contract in this task. Its public surface may be adapted by Task 5.
- `RecordingHttpMessageHandler`: derives from `HttpMessageHandler`, records method/URI/body safely, returns programmable responses, never sends real network traffic, and honors cancellation.
- `ManualTimeProvider`: derives from `TimeProvider`, exposes a controlled UTC clock and explicit advancement; reject invalid backward movement unless the later plan explicitly needs it.

Do not add placeholder methods that assert nothing. Add narrow unit tests for infrastructure only when it performs real normalization/validation/cancellation behavior; the smoke test is the mandatory Task 1 behavior gate.

## Mandatory verification

- Focused smoke RED and GREEN evidence.
- `dotnet test .\tests\RazorReaper.UnitTests\RazorReaper.UnitTests.csproj -c Debug --no-restore --filter "FullyQualifiedName~SmokeTests"` -> exactly one pass.
- `dotnet test .\tests\RazorReaper.UnitTests\RazorReaper.UnitTests.csproj -c Debug --no-restore` -> full Task 1 project green.
- `dotnet build .\RazorReaper\RazorReaper.csproj -c Debug --no-restore` -> App baseline remains green.
- Verify solution contains exactly one App project and one test project; project reference resolves to the exact local App csproj.
- `git diff --check`.
- Task-scoped `git status --short`, `git diff --stat`, and diff for only owned files.
- Explicit scan that no restore contacted or named a remote source, no App process started, and no branch/commit/remote state changed.

## Report

Write `docs/superpowers/.sdd/2026-08-16-app-launch-privacy-plan/task-1-report.md` with exact files, RED/GREEN evidence, any local-only assets-generation command and source proof, test/build output, solution/reference verification, diff checkpoint, and safety-boundary confirmation. Return only status, commits (`none` expected), one-line test/build summary, concerns, and report path.
