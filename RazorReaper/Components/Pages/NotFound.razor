@* No @page directive: the router renders this from Routes.razor's <NotFound> branch, so a
   route that no longer exists shows this instead of an empty window. *@

<header class="page-header">
    <h1 class="page-title">Page not found</h1>
    <p class="page-subtitle">This link points at a page RazorReaper no longer has. Nothing is broken — the page has moved or been retired.</p>
    <a class="rr-pill-btn ghost sm" href="/home">Back to Home</a>
</header>
