@page "/crafting-scripts"
@inject NavigationManager Navigation

@* The Macro / AHK page is gone. It existed to point at AutoHotkey and JitBit — external
   tools the built-in scripts replace, so there's nothing left for it to teach.

   The route stays as a redirect so old links and bookmarks still land somewhere. The
   previous 482-line page is in git history if any of it is ever wanted back. *@

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo("/scripts", replace: true);
}
