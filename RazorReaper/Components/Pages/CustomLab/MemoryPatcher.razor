@* Phase 1 placeholder. Phase 2 builds out the real Coming Soon panel. *@

<div class="lab-placeholder">
    <div class="lab-placeholder-icon">
        <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="4" y="5" width="12" height="10" rx="1.5" />
            <rect x="7.5" y="8.5" width="5" height="3" rx="0.5" />
        </svg>
    </div>
    <h3>Memory Patcher</h3>
    <p>
        Tab unlocked. The Memory Patcher implementation ships in a future update.
    </p>
</div>

<style>
    .lab-placeholder {
        max-width: 540px;
        padding: 2.5rem 2rem;
        text-align: center;
        background: rgba(0, 0, 0, 0.15);
        border: 1px dashed rgba(255, 255, 255, 0.12);
        border-radius: 12px;
        margin: 0 auto;
    }

    .lab-placeholder-icon {
        width: 56px;
        height: 56px;
        border-radius: 14px;
        background: rgba(255, 255, 255, 0.05);
        color: rgba(255, 255, 255, 0.8);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem auto;
    }

    .lab-placeholder-icon svg { width: 28px; height: 28px; }

    .lab-placeholder h3 {
        margin: 0 0 0.4rem 0;
        color: #fff;
        font-size: 1.1rem;
    }

    .lab-placeholder p {
        margin: 0;
        color: rgba(255, 255, 255, 0.65);
        line-height: 1.55;
        font-size: 0.92rem;
    }
</style>
