@* Phase 1 placeholder. Real UI lands in Phase 4 (service layer first in Phase 3). *@

<div class="lab-placeholder">
    <div class="lab-placeholder-icon">
        <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 13a3 3 0 0 1 3-3 4 4 0 0 1 7.8-1A3.2 3.2 0 0 1 17 12.2 2.8 2.8 0 0 1 14.2 15H5.5A2.5 2.5 0 0 1 3 13Z" />
        </svg>
    </div>
    <h3>Sky Injector</h3>
    <p>
        Tab unlocked. The full Sky Injector UI ships in a later update — for now this confirms the lock
        gate works end to end.
    </p>
</div>

<style>
    .lab-placeholder {
        max-width: 540px;
        padding: 2.5rem 2rem;
        text-align: center;
        background: rgba(0, 0, 0, 0.15);
        border: 1px dashed rgba(255, 255, 255, 0.12);
        border-radius: 12px;
        margin: 0 auto;
    }

    .lab-placeholder-icon {
        width: 56px;
        height: 56px;
        border-radius: 14px;
        background: rgba(255, 255, 255, 0.05);
        color: rgba(255, 255, 255, 0.8);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem auto;
    }

    .lab-placeholder-icon svg { width: 28px; height: 28px; }

    .lab-placeholder h3 {
        margin: 0 0 0.4rem 0;
        color: #fff;
        font-size: 1.1rem;
    }

    .lab-placeholder p {
        margin: 0;
        color: rgba(255, 255, 255, 0.65);
        line-height: 1.55;
        font-size: 0.92rem;
    }
</style>
