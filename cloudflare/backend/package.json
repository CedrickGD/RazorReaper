{
	"name": "razorreaper-telemetry-backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "npx vitest",
		"cf-typegen": "wrangler types",
		"cf:whoami": "wrangler whoami",
		"cf:d1:migrate:local": "wrangler d1 migrations apply razorreaper-telemetry-prod --local",
		"cf:d1:migrate:remote": "wrangler d1 migrations apply razorreaper-telemetry-prod --remote"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.3.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.67.0"
	}
}
